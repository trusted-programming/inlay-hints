# inlay-hints
insert inlay_hints of `rust-analyzer` into Rust code

## installation

```bash
cd crates/ide
cargo install --path .
```

# usage
```bash
cd crates/ide
cargo install --path .
ide [<folder>]
```
